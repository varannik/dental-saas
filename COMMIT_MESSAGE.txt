chore(tooling,scripts): add Drizzle tooling and implement docker status command

Prepare Phase 1 foundation work by adding Drizzle migration tooling, updating
pnpm build-script policy, and replacing the docker status stub with a working
implementation used by `make docker-ps`.

## What Changed

- Added `drizzle-kit` to root dev dependencies for schema/migration workflows.
- Updated workspace policy in `pnpm-workspace.yaml`:
  - added `allowBuilds` entry for `esbuild` handling.
- Implemented `scripts/docker/status.sh`:
  - loads shared docker helpers
  - validates Docker + Docker Compose availability
  - gracefully handles daemon not running
  - prints compose service status instead of exiting as a placeholder error
- Added `packages/config/package.json` scaffold to bootstrap shared config package setup.

## Files Updated

- `package.json`
- `pnpm-workspace.yaml`
- `scripts/docker/status.sh`
- `packages/config/package.json` (new)
