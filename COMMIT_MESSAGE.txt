feat(config,types,db): finalize schema foundation, migrations, and seeding

Complete Phase 1 core foundation by hardening config/types packages, fixing Drizzle migration blockers,
enabling pgvector locally, and adding an idempotent seed script aligned with the implemented schema.

## What Changed

- Finalized `@saas/config` package setup:
  - Replaced scaffold package metadata with workspace-ready config.
  - Added stable exports for env/database/schema utilities.
- Hardened runtime config and DB setup:
  - Improved env loading behavior and cache controls in `env.ts`.
  - Improved DB connection/pool lifecycle and typed schema wiring in `database.ts`.
- Added Drizzle project config:
  - Introduced root `drizzle.config.ts` with env-aware DB URL loading.
- Fixed migration blockers:
  - Updated pgvector index op class on `agent_memories.embedding` to `vector_cosine_ops`.
  - Removed non-immutable `NOW()` predicates from partial index definitions used by migrations.
- Enabled pgvector support in local Docker Postgres:
  - Switched Postgres image to `pgvector/pgvector:pg16` in compose config.
  - Verified extension availability in local instance.
- Completed schema migration generation and application path:
  - Regenerated initial migration SQL in `drizzle/`.
  - Verified full table creation in local DB.
- Built out `@saas/types` package:
  - Added domain type modules and root barrel exports.
  - Synced interfaces to implemented Drizzle schema and fixed NodeNext `.js` path requirements.
  - Resolved lint errors from stale staged imports.
- Added seed workflow:
  - Created `packages/config/src/seed.ts` with idempotent default tenant/user/roles/permissions/sample patients.

## Outcome

- Expected schema tables from Drizzle definitions: 108
- Actual tables in local `public` schema: 108
- Missing/extra table mismatch: 0 / 0
- Type/schema entity mapping mismatch (audited): 0

## Validation

- `pnpm --filter @saas/config typecheck` passes.
- `pnpm --filter @saas/types lint` passes.
- `pnpm --filter @saas/types typecheck` passes.
- `pnpm exec tsc --noEmit --module NodeNext --moduleResolution NodeNext --target ES2022 --strict src/*.ts` (from `packages/types`) passes.
- Local DB verification confirms all 108 schema tables exist.
