feat(config,types): establish schema-aligned shared packages

Create production-ready `@saas/config` and `@saas/types` foundations, then sync
types to Drizzle schema fields and NodeNext ESM import rules for consistent cross-service contracts.

## What Changed

- Standardized workspace package setup for `packages/config`:
  - Replaced scaffolded `package.json` with scoped internal package metadata and scripts.
  - Added typed exports for env/database/schema usage.
- Hardened environment config:
  - Improved dotenv load order by environment.
  - Added cache reset/reload support for tests and runtime refresh.
- Hardened database config:
  - Added schema-typed Drizzle client wiring.
  - Added safer pool lifecycle behavior and shutdown handling.
  - Added richer connection options (timeouts, keepalive, SSL mode handling).
- Created and expanded `packages/types` package:
  - Added complete domain type modules and root barrel export.
  - Added package exports for all major domains.
- Synced type interfaces to implemented schema:
  - Regenerated `packages/types/src/*` interfaces to match `packages/config/src/schema/*` fields.
  - Resolved field-level drift across all mapped entities.
- Fixed NodeNext import compatibility:
  - Updated relative imports/exports in `packages/types/src` to explicit `.js` paths.

## Sync Outcome

- Mapped schema entities audited: 108
- Field-level mismatches after sync: 0
- `packages/types/src` NodeNext import extension issues: resolved

## Validation

- `pnpm --filter @saas/config typecheck` passes.
- `pnpm --filter @saas/types typecheck` passes.
- `pnpm exec tsc --noEmit --module NodeNext --moduleResolution NodeNext --target ES2022 --strict src/*.ts` (from `packages/types`) passes.
